# 웹 대시보드 사용 가이드

## 📁 추가된 파일

```
meotsa_project/
├── app.py                    # Flask 웹 서버
├── templates/
│   └── index.html           # 대시보드 HTML
└── static/
    ├── style.css            # 스타일시트
    └── app.js               # 프론트엔드 로직
```

## 🚀 실행 방법

### 1. 의존성 설치 (Flask 추가)

```bash
pip install -r requirements.txt
```

### 2. 웹 서버 실행

```bash
python app.py
```

### 3. 브라우저 접속

```
http://localhost:5000
```

## 🎯 기능

### 1. 분석 시작
- **"🚀 분석 시작"** 버튼 클릭
- 실시간 진행 상황 표시
- 현재 분석 중인 기사 제목 표시

### 2. 통계 카드
- **🔴 High**: 높은 투자 적합도
- **🟡 Medium**: 중간 투자 적합도
- **🟢 Low**: 낮은 투자 적합도

### 3. 결과 테이블
- 등급별 색상 구분
- 사건 분야, 상대방, 피해 규모 표시
- 기사 링크 클릭 가능

### 4. 상세 정보
- **"상세"** 버튼으로 모달 팝업
- 요약, 판단 근거, 충족 조건 확인

### 5. 필터링
- 등급별 필터 (전체/High/Medium/Low)

## 📊 API 엔드포인트

- `POST /api/analyze` - 분석 시작
- `GET /api/status` - 진행 상황 조회
- `GET /api/results?grade=<등급>` - 결과 조회

## 💡 주요 특징

✅ **실시간 업데이트**: 분석 진행 상황을 1초마다 갱신  
✅ **반응형 디자인**: 모바일/태블릿/데스크톱 지원  
✅ **모던 UI**: 그라데이션 디자인과 부드러운 애니메이션  
✅ **국내 뉴스 전용**: 해외 기사 자동 필터링  
✅ **자동 저장**: JSON 파일로 결과 자동 저장

## ⚠️ 주의사항

- 분석 중에는 **"분석 시작"** 버튼이 비활성화됩니다
- 브라우저를 닫아도 백그라운드 분석은 계속됩니다
- 서버를 중단하려면 터미널에서 `Ctrl+C`를 누르세요
